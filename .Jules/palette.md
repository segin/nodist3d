## 2024-05-30 - TimelineUI Accessibility
**Learning:** Adding dynamic ARIA attributes (like `aria-pressed` and changing `aria-label`) to toggle buttons provides crucial feedback for screen readers without altering the visual design.
**Action:** Always check toggle buttons for missing state indicators for assistive tech.
